import { Meta, Story, Props, Preview } from '@storybook/addon-docs/blocks'
import { Button } from '../index'
import * as stories from './button.stories'

<Meta title="Button" component={Button} />

# Button

Default Button

<Preview>
  <Story name="default" parameters={{ docs: { source: { code: '<Button>Default</Button>' } } }}>
    {stories.defaultView()}
  </Story>
</Preview>

Primary Button

<Preview>
  <Story name="primary" parameters={{ docs: { source: { code: '<Button type="primary">Primary</Button>' } } }}>
    {stories.primaryView()}
  </Story>
</Preview>

Dash Button

<Preview>
  <Story name="dashed" parameters={{ docs: { source: { code: '<Button type="dashed">Dash</Button>' } } }}>
    {stories.dashView()}
  </Story>
</Preview>

Link Button

<Preview>
  <Story name="link" parameters={{ docs: { source: { code: '<Button type="link">Link</Button>' } } }}>
    {stories.linkView()}
  </Story>
</Preview>

## Properties

<Props of={Button} />
