import { Meta, Story, Props, Preview } from '@storybook/addon-docs/blocks';
import { Input } from '../index';

<Meta title='Input' component={Input} />

# Input
Input component which is wrapper of ant checkbox with full properties from ant design
<Preview>
  <Story name="default">
      <Input/>
  </Story>
</Preview>

## Props
<Props of={Input} />
